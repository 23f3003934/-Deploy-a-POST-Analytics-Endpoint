# -Deploy-a-POST-Analytics-Endpoint
TDS ga 2 q5 Deploy a stateless POST /analytics endpoint that authenticates via your assigned API key and aggregates a batch of events. The grader sends a fresh randomized event batch on every check — you must compute results, not hard-code them.
